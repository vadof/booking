stages:
  - build
  - deploy

build:
  stage: build
  only:
    refs:
      - main
  tags:
    - server
  script:
    - echo "Start Build"
    - cd backend
    - chmod +x mvnw
    - ls
    - ls target
    - ./mvnw clean package
    - ls target

deploy:
  stage: deploy
  only:
    refs:
      - main
  tags:
    - server
  script:
    - echo 'Start deploy'
    - pwd
    - ls
    - docker-compose down
    - docker-compose up -d
    - docker-compose ps
